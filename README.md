# Farrgut Frontend

## Implemented with React and Typescript

## Prerequisites

- Node.js 20 lts
- npm

## Installation

- Clone the repository
- `cd` into `farragut-fe`
- Run `npm install`
- Run `npm start`